// Grafana Alloy reference config for logos-indexer.
// All values are environment placeholders; provide them in the Alloy runtime environment.

prometheus.scrape "logos_indexer" {
  targets = [{
    __address__ = env("LOGOS_INDEXER_METRICS_TARGET"),
    job         = "logos-indexer",
  }]
  scrape_interval = env("LOGOS_INDEXER_SCRAPE_INTERVAL")
  forward_to      = [prometheus.remote_write.default.receiver]
}

prometheus.remote_write "default" {
  endpoint {
    url = env("PROMETHEUS_REMOTE_WRITE_URL")

    basic_auth {
      username = env("PROMETHEUS_REMOTE_WRITE_USERNAME")
      password = env("PROMETHEUS_REMOTE_WRITE_PASSWORD")
    }
  }
}

local.file_match "logos_indexer_logs" {
  path_targets = [{
    __path__ = env("LOGOS_INDEXER_LOG_PATH"),
    job      = "logos-indexer",
  }]
}

loki.source.file "logos_indexer" {
  targets    = local.file_match.logos_indexer_logs.targets
  forward_to = [loki.process.logos_indexer.receiver]
}

loki.process "logos_indexer" {
  stage.json {
    expressions = {
      ts       = "ts",
      level    = "level",
      label    = "label",
      message  = "message",
      metadata = "metadata",
    }
  }

  stage.labels {
    values = {
      level = "level",
      label = "label",
    }
  }

  stage.timestamp {
    source = "ts"
    format = "RFC3339Nano"
  }

  forward_to = [loki.write.default.receiver]
}

loki.write "default" {
  endpoint {
    url = env("LOKI_WRITE_URL")

    basic_auth {
      username = env("LOKI_WRITE_USERNAME")
      password = env("LOKI_WRITE_PASSWORD")
    }
  }
}
